# # ============================================================
# # Causal Context Pruning (CCP) — Dependencies
# # ============================================================

# # Core LangGraph / LangChain stack
# langgraph>=0.2.0
# langchain>=0.2.0
# langchain-openai>=0.1.0

# # OpenAI-compatible client (used for Nautilus endpoint)
# openai>=1.0.0

# # Retry logic (matches user's existing wrapper)
# tenacity>=8.2.0

# # Tokenisation utility
# tiktoken>=0.7.0

# # AppWorld benchmark
# appworld>=0.1.3

# # Numerical / data tools
# numpy>=1.26.0

# # Testing
# pytest>=8.0.0
# pytest-mock>=3.12.0

# # Optional: progress bars for long experiment runs
# tqdm>=4.66.0

# # Optional: result plotting (ablation curves)
# matplotlib>=3.8.0
# pandas>=2.2.0

# ============================================================
# Causal Context Pruning (CCP) — Dependencies
# ============================================================
#
# NOTE: appworld is intentionally excluded here.
# It requires pydantic<2 which conflicts with langgraph/langchain.
# Install it separately in the job scripts with:
#   pip install appworld --no-deps
#   pip install pydantic==1.10.21 sqlmodel==0.0.10
# ============================================================

# ============================================================
# Causal Context Pruning (CCP) — Dependencies
# ============================================================
#
# NOTE: appworld is intentionally excluded here.
# It requires pydantic<2 which conflicts with langgraph/langchain.
# Installed separately in job scripts via an isolated venv.
# ============================================================

# Core LangGraph / LangChain stack
# langchain-core omitted — resolved automatically as a transitive dep
langgraph>=1.0.0
langchain>=1.0.0
langchain-openai>=1.0.0

# OpenAI-compatible client
openai>=1.50.0

# MCP protocol
mcp>=1.0.0
langchain-mcp-adapters>=0.2.0

# Retry logic
tenacity>=8.2.0

# HTTP client (OfficeBench REST, DuckDuckGo fallback)
requests>=2.31.0

# NQ dataset loading
datasets>=2.14.0

# Tokenisation
tiktoken>=0.7.0

# Numerical / data
numpy>=1.26.0
pandas>=2.1.0

# Plotting
matplotlib>=3.8.0

# Testing
pytest>=8.0.0
pytest-mock>=3.12.0

# Progress bars
tqdm>=4.66.0
