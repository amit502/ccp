# ============================================================
# Causal Context Pruning (CCP) — Dependencies
# ============================================================

# Core LangGraph / LangChain stack
langgraph>=0.2.0
langchain>=0.2.0
langchain-openai>=0.1.0

# OpenAI-compatible client (used for Nautilus endpoint)
openai>=1.0.0

# Retry logic (matches user's existing wrapper)
tenacity>=8.2.0

# Tokenisation utility
tiktoken>=0.7.0

# AppWorld benchmark
appworld>=0.1.3

# Numerical / data tools
numpy>=1.26.0

# Testing
pytest>=8.0.0
pytest-mock>=3.12.0

# Optional: progress bars for long experiment runs
tqdm>=4.66.0

# Optional: result plotting (ablation curves)
matplotlib>=3.8.0
pandas>=2.2.0
